{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm ci --prefer-offline=false || npm install --prefer-offline=false",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
